
<%= simple_form_for [@question, @user_answer] do |f| %>
  <%= f.association :answer, as: :hidden, input_html: {value: answer.id} %>
  <%= f.submit answer.content, class: 'btn btn-info mt-2' %>
<% end %>
